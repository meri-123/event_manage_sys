<%= form_with model: [ @management, @management.comments.build ] do |form| %>
  <%# <p>
    <%= form.label :commenter %><br>
    <%# <%= form.text_field :commenter %>

  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>